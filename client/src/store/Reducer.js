const initialState = {
    table: '', // Selected Table
    tableData: [], // Selected table data
    columnName: [], // Columns name of current selected table. 
}

// Tempat milah. Ditentuin sama nama action.
const reducer = (state = initialState, action) => {

}